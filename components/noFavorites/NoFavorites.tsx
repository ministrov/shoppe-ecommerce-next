import styles from './NoFavorites.module.css';

/**
 * Компонент для отображения состояния, когда в избранном нет товаров.
 * Показывает сообщение о том, что пользователь ещё не добавил ничего в избранное.
 * Используется на странице избранного для улучшения пользовательского опыта.
 *
 * @returns {JSX.Element} Контейнер с заголовком-сообщением
 *
 * @example
 * // Использование на странице избранного
 * {favorites.length === 0 && <NoFavorites />}
 */
export const NoFavorites = () => {
  return (
    <div className={styles.noFavorites}>
      <h1>Вы еще не добавили ничего в избранное!!!</h1>
    </div>
  )
}
