import type { Metadata } from 'next';
import { Header } from '@/layouts/header/Header';
import { Footer } from '@/layouts/footer/Footer';
import { AuthInitializer } from '@/components/authInitializer/AuthInitializer';
import { PageTransitions } from '@/components/pageTransitons/PageTransitions';
import { DevAuthSwitch } from '@/components/devAuthSwitch/DevAuthSwitch';
import { GeolocationContainer } from '@/components/geolocationContainer/GeolocationContainer';
import StoreProvider from './StoreProvider';
import { cuprum } from '@/styles/fonts';
import './globals.css';

/**
 * Метаданные для всего приложения.
 * Определяют заголовок и описание, которые используются по умолчанию.
 */
export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
};

/**
 * Корневой layout приложения.
 * Оборачивает всё приложение в провайдеры и содержит общие элементы:
 * Header, Footer, AuthInitializer, GeolocationContainer, PageTransitions и DevAuthSwitch.
 *
 * @param {Object} props - Свойства layout'а
 * @param {React.ReactNode} props.children - Дочерние элементы (страницы)
 * @returns {JSX.Element} Корневой layout
 */
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang='en' className={cuprum.variable}>
      <body>
        <div className='container'>
          <StoreProvider>
            <Header />
            <AuthInitializer />
            <GeolocationContainer />
            <main>
              <PageTransitions>
                {children}
              </PageTransitions>
            </main>
            <Footer />
            <DevAuthSwitch />
          </StoreProvider>
        </div>
      </body>
    </html>
  );
}

